node_modules/
.next/
next-env.d.ts
out/
build/
coverage/
playwright-report/
test-results/
.lighthouseci/
package-lock.json
liacup.css

# Documentos de governanca e artefatos do Spec Kit: o Prettier nao formata o que
# a liga aprovou nem o que o processo gerou. Conteudo desses arquivos e decidido
# por pessoas, nao normalizado por ferramenta (Principio VI).
docs/
specs/
.specify/

# Tipos GERADOS a partir do esquema do banco (F02 D5). O Prettier nao os formata
# por um motivo mecanico, nao estetico: `banco:tipos:check` compara este arquivo
# com o que a ferramenta do Supabase produz agora. Formatar aqui faria a
# comparacao divergir para sempre, e um passo do CI que so pode falhar nao mede
# mais nada.
src/lib/supabase/tipos.ts

# Estado local da ferramenta do Supabase: ref do projeto ligado, versoes. Nao e
# codigo do projeto e nao vai para o repositorio.
supabase/.temp/
